---
name: phase-router
description: "Cantrip 工作流入口，意图识别与阶段路由。当用户说「用 cantrip」或「用 phase-router」「实现/开发/添加功能」「修复 bug」「修改/优化」或「记录经验」时激活。按任务类型路由到对应 agent。"
---

# Phase-Router（入口 Skill）

Cantrip 是管理需求、技术设计、代码实现、Bug 修复与经验沉淀的开发工作流系统。本 skill 负责意图识别、术语解析、功能存在性检查与路由。

## 何时使用本 Skill

在以下情况激活本 skill：
- 用户说「用 cantrip」或「用 phase-router」或「用 cantrip 做…」「用 phase-router 做…」
- 用户发起开发任务：实现/开发/添加/新建
- 用户报告 bug：修复/解决/bug/问题/报错
- 用户要修改：修改/调整/变更/更新
- 用户要改进：重构/优化/改进
- 用户要保存经验：记录/沉淀/remember/save experience

**重要**：同一对话内一旦激活，后续消息均视为当前任务的延续，无需重复说「用 cantrip」或「用 phase-router」，除非开启全新任务。

## 前置检查

### 步骤 0：检查是否已初始化

检查 `.cantrip/` 与 `.cantrip/context/project/overview.md` 是否存在；若缺失则调用 **init-project** 并等待完成。

**调用 init-project 时**：init-project 必须先向用户提问（项目名称、描述等）并收集回答，再创建任何文件；不得在未询问用户的情况下直接生成文档。

### 步骤 1：解析术语

调用 **resolve-term**：将用户说法转为规范名，供后续 index-feature 使用。查 `.cantrip/context/rules/term-mappings.md` 获取已有映射。

### 步骤 2：检查功能是否已存在

用规范名调用 **index-feature**。在 `.cantrip/context/features/` 中检索：
- 若已存在：询问用户 [修改] [查看详情] [做别的功能]
- 若不存在：按新功能流程继续

### 步骤 3：检查进行中任务

读取 `.cantrip/workspace/index.md`：
- 若有匹配的进行中任务：读取其 status.md 并询问是否继续
- 若为新任务：创建新任务工作区

### 步骤 4：路由到对应 Agent

按任务类型交给对应 agent。路由前可调用 **index-experience**、加载 **overview.md**，将上下文传给目标 agent。

## 任务类型与决策逻辑

| 关键词 | 类型 | 路由到 |
|--------|------|--------|
| implement, develop, add, create, new, extend | 新功能 | requirement-manager |
| fix, solve, bug, issue, error, crash, lag | Bug 修复 | fix-agent |
| modify, adjust, change, update, alter | 修改 | design-manager |
| refactor, optimize, improve | 优化 | design-manager |
| continue, resume, restore | 继续 | 读取状态并继续 |
| remember, save experience, record lesson | 经验沉淀 | experience-depositor |

| 任务类型 | Sub-Agent | 起始阶段 |
|----------|-----------|----------|
| 新功能 | requirement-manager | analyzing |
| Bug 修复 | fix-agent | analyzing |
| 修改 | design-manager | designing |
| 优化 | design-manager | designing |
| 继续 | （读状态） | （当前阶段） |
| 经验沉淀 | experience-depositor | collecting |

## 完成条件

- 任务类型已划分并展示给用户
- 规范术语已解析（如适用）
- 功能存在性检查已完成
- 任务已路由到正确 agent
- `.cantrip/workspace/{task-id}/status.md` 已初始化（新任务时）

## 输出格式

```
📋 任务分析

**类型**: {新功能 / Bug 修复 / 修改 / 优化 / 继续 / 经验沉淀}
**术语解析**: {用户说法} → {规范名}
**功能检查**: {未找到 / 已存在}
**状态**: {新任务 / 进行中}
**Task ID**: {generated-id}
**路由到**: {agent-name}

🚀 正在进入 {agent-name}...
```

## Sub-Agents 参考

- **requirement-manager**：需求分析与规格
- **design-manager**：技术设计与架构
- **implementation-executor**：代码实现
- **fix-agent**：Bug 修复流程
- **experience-depositor**：经验沉淀

## 对话延续

激活后：
- 后续消息均视为当前任务的一部分
- 用户可说「继续」「下一步」「改这里」而无需重复「用 cantrip」或「用 phase-router」
- 任务状态保存在 `.cantrip/workspace/{task-id}/status.md`
- 仅在开启全新任务时再次说「用 cantrip」或「用 phase-router」
